export class Formation {
    public year: number;
    public title: string;
    public school: string;

    constructor(year: number, title: string, school: string) {
        this.year = year;
        this.title = title;
        this.school = school;
    }
}